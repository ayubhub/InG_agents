# Руководство по отслеживанию и сбросу лимитов LinkedIn

## 📋 Оглавление
1. [Как отслеживать лимиты](#как-отслеживать-лимиты)
2. [Как сбросить лимиты](#как-сбросить-лимиты)
3. [Понимание лимитов LinkedIn](#понимание-лимитов-linkedin)
4. [Автоматическое отслеживание](#автоматическое-отслеживание)

## 🔍 Как отслеживать лимиты

### Использование скрипта мониторинга

Самый простой способ - использовать скрипт `monitor_linkedin_limits.py`:

```bash
# Показать текущий статус всех аккаунтов
python monitor_linkedin_limits.py

# Или явно указать команду
python monitor_linkedin_limits.py status
```

Скрипт покажет:
- ✅ **Дневной лимит**: сколько приглашений отправлено из доступных (по умолчанию 50/день)
- ⏰ **Часовой лимит**: сколько отправлено за текущий час (по умолчанию 10/час)
- ⏳ **Cooldown**: время до сброса временных ограничений
- ⚠️ **Ошибки**: количество ошибок для каждого аккаунта
- 📊 **Статистика**: общее количество отправленных приглашений

### Просмотр файла состояния напрямую

Состояние хранится в файле: `data/state/multi_account_state.json`

```bash
# Просмотр файла
cat data/state/multi_account_state.json
```

Структура файла:
```json
{
  "account_stats": {
    "Account_1": {
      "daily_sent": 25,           // Отправлено сегодня
      "hourly_sent": 5,            // Отправлено за текущий час
      "last_reset_date": "2025-11-28",  // Дата последнего сброса
      "last_reset_hour": 14,       // Час последнего сброса
      "total_sent": 150,           // Всего отправлено
      "error_count": 0,            // Количество ошибок
      "cooldown_until": null,      // Время до сброса cooldown
      "last_error": null,          // Последняя ошибка
      "last_used": "2025-11-28T14:30:00"  // Время последнего использования
    }
  },
  "current_account_index": 0,     // Индекс текущего активного аккаунта
  "global_cooldown_until": null   // Глобальный cooldown (для всех аккаунтов)
}
```

## 🔄 Как сбросить лимиты

### ⚠️ ВАЖНО: Что можно и нельзя сбросить

**✅ Можно сбросить (локальные счетчики):**
- Счетчики дневных/часовых отправок в вашей системе
- Счетчики ошибок
- Временные cooldown'ы

**❌ НЕЛЬЗЯ сбросить (ограничения LinkedIn):**
- Реальные лимиты LinkedIn на их серверах
- Недельные лимиты приглашений LinkedIn
- Блокировки аккаунта от LinkedIn

### Сброс через скрипт

```bash
# Сбросить все аккаунты
python monitor_linkedin_limits.py reset

# Сбросить конкретный аккаунт
python monitor_linkedin_limits.py reset Account_1
```

### Сброс через существующий скрипт

```bash
# Использовать готовый скрипт сброса
python reset_linkedin_accounts.py
```

### Ручной сброс (редактирование файла)

1. Откройте `data/state/multi_account_state.json`
2. Для нужного аккаунта установите:
   - `"daily_sent": 0`
   - `"hourly_sent": 0`
   - `"error_count": 0`
   - `"cooldown_until": null`
3. Сохраните файл

## 📊 Понимание лимитов LinkedIn

### Типы лимитов

1. **Дневной лимит** (настраивается через `LINKEDIN_DAILY_LIMIT`)
   - По умолчанию: 50 приглашений/день
   - Сбрасывается автоматически каждый день в 00:00

2. **Часовой лимит** (настраивается через `LINKEDIN_HOURLY_LIMIT`)
   - По умолчанию: 10 приглашений/час
   - Сбрасывается автоматически каждый час

3. **Недельный лимит LinkedIn** (устанавливается LinkedIn)
   - Обычно: 100-200 приглашений/неделю
   - Сбрасывается LinkedIn в начале новой недели
   - **НЕ контролируется вашей системой**

4. **Cooldown** (временная блокировка)
   - Устанавливается при ошибках типа "temporary provider limit"
   - Индивидуальный: 10 минут для одного аккаунта
   - Глобальный: 20 минут для всех аккаунтов

### Ошибки LinkedIn

**"You've reached the weekly invitation limit"**
- Достигнут недельный лимит LinkedIn
- Решение: подождать до начала новой недели (обычно понедельник)

**"Cannot resend yet - You have reached a temporary provider limit"**
- Временное ограничение от провайдера (Unipile)
- Решение: система автоматически устанавливает cooldown на 10-20 минут

**"Rate limit exceeded"**
- Превышен лимит запросов к API
- Решение: система автоматически переключается на другой аккаунт

## 🤖 Автоматическое отслеживание

Система автоматически отслеживает лимиты через класс `MultiAccountLinkedInSender`:

### Автоматические функции

1. **Автоматический сброс счетчиков**
   - Дневные счетчики сбрасываются при смене даты
   - Часовые счетчики сбрасываются при смене часа

2. **Автоматическое переключение аккаунтов**
   - При достижении лимита одного аккаунта, система переключается на другой
   - Если все аккаунты недоступны, возвращается ошибка

3. **Автоматический cooldown**
   - При ошибках типа "temporary provider limit" устанавливается cooldown
   - Система автоматически ждет окончания cooldown перед повторной попыткой

### Настройка лимитов

Через переменные окружения в `.env`:

```env
# Дневной лимит на аккаунт
LINKEDIN_DAILY_LIMIT=50

# Часовой лимит на аккаунт
LINKEDIN_HOURLY_LIMIT=10
```

### Программный доступ к статусу

```python
from src.integrations.multi_account_linkedin import MultiAccountLinkedInSender

# Инициализация
sender = MultiAccountLinkedInSender(config)

# Получить статус всех аккаунтов
status = sender.get_account_status()
print(status)
```

## 📝 Рекомендации

1. **Регулярно проверяйте статус**
   ```bash
   python monitor_linkedin_limits.py
   ```

2. **Не превышайте рекомендуемые лимиты**
   - Начинайте с 20-25 приглашений/день для новых аккаунтов
   - Постепенно увеличивайте до 40-60/день

3. **Персонализируйте приглашения**
   - Избегайте массовых рассылок
   - Персонализация снижает риск блокировки

4. **Используйте несколько аккаунтов**
   - Система автоматически распределяет нагрузку
   - При блокировке одного аккаунта используется другой

5. **Мониторьте ошибки**
   - Регулярно проверяйте `last_error` в статусе
   - При частых ошибках снизьте активность

## 🔧 Устранение проблем

### Все аккаунты недоступны

1. Проверьте глобальный cooldown:
   ```bash
   python monitor_linkedin_limits.py
   ```

2. Если cooldown истек, но аккаунты все еще недоступны:
   ```bash
   python monitor_linkedin_limits.py reset
   ```

3. Проверьте, не достигнут ли недельный лимит LinkedIn

### Лимиты не сбрасываются автоматически

1. Проверьте правильность даты/времени на сервере
2. Убедитесь, что файл состояния доступен для записи
3. Проверьте логи на наличие ошибок

### Частые ошибки "temporary provider limit"

1. Снизьте `LINKEDIN_DAILY_LIMIT` и `LINKEDIN_HOURLY_LIMIT`
2. Увеличьте интервалы между отправками
3. Проверьте, не используете ли вы слишком много аккаунтов одновременно



